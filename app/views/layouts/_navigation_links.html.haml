%li= link_to 'Badge Library', badges_path
- if user_signed_in?
  - if current_user.is_admin? || current_user.is_librarian?
    %li= link_to 'Add Badge', new_badge_path 
  - else
    %li= link_to 'Propose Badge', new_badge_path 
%li= link_to 'Badge Holders', users_path
%li
  %a{"data-dropdown" => "hover1", "data-options" => "is_hover:true; hover_timeout:5000", :href => "#"} Nominate For ...
  %ul#hover1.f-dropdown.medium{"data-dropdown-content" => ""}
    - Badge.accepted.order(:name).each do |b|
      = render '/badges/dropdown_item', badge: b
%li= link_to 'Validate', new_nomination_vote_path
- if user_signed_in?
  %li= link_to "Sign out (#{current_user.try(:role)}) #{current_user.try(:name)}", signout_path
- else
  %li= link_to 'Sign in with your H1 Gmail', signin_path
