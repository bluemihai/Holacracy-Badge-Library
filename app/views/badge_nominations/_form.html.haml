= simple_form_for(@badge_nomination) do |f|
  = f.error_notification

  .form-inputs
    #user_id= f.association :nominator, label: 'Nominator', include_blank: true, selected: @nominator.try(:id)
    #user_id= f.association :user, label: 'Nominee'
    - if params[:badge_id]
      = f.hidden_field :badge_id, value: params[:badge_id]
    - else
      #badge_id= f.association :badge, collection: Badge.accepted.order(:name), label_method: :name_with_focus
    - if @badge.has_no_levels?
      #level= f.input :level_nominated, disabled: true, label: 'This badge has no levels.'
    - else
      #level= f.input :level_nominated, collection: @badge.detailed_levels
    = f.input :evidence, label: 'Evidence and examples for this nomination'
    - if current_user.try(:is_librarian?) || current_user.try(:is_admin?)
      %div.admin-only
        = f.input :status, collection: [['Pending', 'pending'], ['Accepted', 'accepted'], ['Expired', 'expired']]
        = f.input :level_granted, collection: @badge.detailed_levels
    - else
      = f.input :status, collection: [['Pending', 'pending']]      

  .form-actions
    = f.button :submit, 'Create/Update Badge Nomination'
